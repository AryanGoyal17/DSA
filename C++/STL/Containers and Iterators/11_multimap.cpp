#include <bits/stdc++.h>
using namespace std;

int main(){

    //Multi-map -- Same as map... but the keys are not unique..

    // keys-- sorted, not unique
    // values -- not unique..

    //Similar to the relationship between set and multiset..

    //Initializing --

    multimap <int, int> mm1;

    return 0;
}