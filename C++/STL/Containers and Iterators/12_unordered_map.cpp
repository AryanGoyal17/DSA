#include <bits/stdc++.h>
using namespace std;

int main(){

    //Similar to relationship between set and unordered set...

    //keys -- unique, but not sorted..
    //values -- not unique

    //Initializing --

    unordered_map <int, int> um1;

    //[] can be used to access values....


    //NOTE: Time complexity of almost all unordered map ops = O(1), constant time.. except in worst case(collision) -- O(N), linear..
    //NOTE: Time complexity of almost all map ops -- O(logn)


    //NOTE---- FOR ALL C++ DATA STRUCTURES-- 
    // ------- emplace ops(emplace, emplace_back, emplace_front etc..) are faster than insert/push ops(push_back, insert, push_front, etc...)

    return 0;
}